# JNB Survivor (PixiJS + Vite + TypeScript)

体验地址: https://benqy.github.io/jnb/

## 开发

```powershell
pnpm install
pnpm dev
```

打开提示的本地地址（默认 `http://localhost:2334`）。

## 操作

- `W/A/S/D`：移动
- 升级时：按 `1/2/3` 或点击卡牌选择

## 构建

```powershell
pnpm build
pnpm preview
```
